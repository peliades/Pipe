1) no terminal, executar "make" com a pasta aberta

2) comandos:

    "make run": executa o programa 

    "make clean": limpa os arquivos criados

ESTA CORRETO:

porque verifiquei com o getpid() que os filhos estão lendo as mensagens dos pais, e 
o pai não esta lendo a propria mensagens.
